#' @title orders the increasing  nominal group
#' @description orders the change in clonal stage as increasing between specific clonal stage 2 or 3, where pre-leukemic stage is low
#' @param mt a matrix of collapse bundles kexp
#' @param globalMin the selected global min column number
#' @param globalMid the column number of desired mid
#' @param increaseStage is either the first or second stage with constant othewise
#' @return a matrix group that fits into this nominal group ordering
#' @export
orderIncreasing<-function(mt, globalMax=stageId1,globalMin=stageId2,globalMid=stageId3,increaseStage=stageId2){
 
  



} ###{main
